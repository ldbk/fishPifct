###
# generated on 2015-10-20 21:52:08
###

setClass(
	Class="csPi",
	slots=list(
		classVersion="character",
		desc="character",
		popData="character",
		design="character",
		se="data.frame",
		tr="data.frame",
		hh="data.frame",
		sl="data.frame",
		hl="data.frame",
		ca="data.frame"
	),
	prototype=list(
		classVersion="2.1",
		desc="Commercial Sampling Data format for the fishPi project",
		popData="Named population data object",
		design="Design description",
		se=data.frame(
			recType=character(), 
			seCode=character(), 
			dataProv=character(), 
			sampCtry=character(), 
			sampInst=character(), 
			sampMeth=character(), 
			sampTeam=character(), 
			seYear=integer(), 
			sampDate=character(), 
			sampTime=character(), 
			sampLoc=character(), 
			sampLocType=character(), 
			psuType=character(), 
			design=character(), 
			popData=character(), 
			sampScheme=character(), 
			sampStrata=character(), 
			sampTemporalUnit=character(), 
			sampTemporalId=character(), 
			psuKey=character(), 
			psuId=character(), 
			psuTotal=character(), 
			psuSampled=character(), 
			psuSampProb=character()
		), 
		tr=data.frame(
			recType=character(), 
			seCode=character(), 
			year=integer(), 
			proj=character(), 
			trpCode=character(), 
			sampType=character(), 
			vslFlgCtry=character(), 
			vslId=integer(), 
			vslLen=integer(), 
			vslLenCat=character(), 
			vslPwr=integer(), 
			vslSize=integer(), 
			vslSizeUnit=character(), 
			vslType=integer(), 
			foNum=integer(), 
			daysAtSea=integer(), 
			voyageId=character(), 
			depLoc=character(), 
			depDate=character(), 
			depTime=character(), 
			arvLoc=character(), 
			arvDate=character(), 
			arvTime=character(), 
			ssuType=character(), 
			ssuKey=character(), 
			ssuId=character(), 
			ssuTotal=character(), 
			ssuSampled=character(), 
			ssuSampProb=character()
		), 
		hh=data.frame(
			recType=character(), 
			seCode=character(), 
			year=integer(), 
			proj=character(), 
			trpCode=character(), 
			aggLev=character(), 
			landFrac=character(), 
			staNum=integer(), 
			foType=character(), 
			foKey=character(), 
			foId=character(), 
			foVal=character(), 
			catReg=character(), 
			sppReg=character(), 
			foDate=character(), 
			foTime=character(), 
			foDur=integer(), 
			latIni=double(), 
			lonIni=double(), 
			latFin=double(), 
			lonFin=double(), 
			ecoZone=character(), 
			area=character(), 
			rect=character(), 
			subRect=character(), 
			foDep=integer(), 
			waterDep=integer(), 
			foCatNat=character(), 
			foCatEu5=character(), 
			foCatEu6=character(), 
			gear=character(), 
			meshSize=integer(), 
			selDev=integer(), 
			meshSizeSelDev=integer(), 
			landCtry=character(), 
			landLoc=character(), 
			landLocType=character(), 
			landDate=character(), 
			landTime=character(), 
			saleCtry=character(), 
			saleLoc=character(), 
			saleDate=character(), 
			saleTime=character(), 
			buyerLoc=character(), 
			domain1=character(), 
			domain2=character(), 
			foTotal=character(), 
			foSampled=character(), 
			foSampProb=character()
		), 
		sl=data.frame(
			recType=character(), 
			seCode=character(), 
			year=integer(), 
			proj=character(), 
			trpCode=character(), 
			staNum=integer(), 
			foId=character(), 
			commSpp=character(), 
			spp=character(), 
			catchCat=character(), 
			landCat=character(), 
			commCatScl=character(), 
			commCat=integer(), 
			subSampCat=character(), 
			sex=character(), 
			unitType=character(), 
			unitKey=character(), 
			unitId=character(), 
			wt=integer(), 
			subSampWt=integer(), 
			totWtDeriv=character(), 
			sampWtDeriv=character(), 
			measType=character(), 
			pres=character(), 
			convFacWt=character(), 
			lenCode=character(), 
			unitTotal=character(), 
			unitSampled=character(), 
			unitSampProb=character()
		), 
		hl=data.frame(
			recType=character(), 
			seCode=character(), 
			year=integer(), 
			proj=character(), 
			trpCode=character(), 
			staNum=integer(), 
			foId=character(), 
			spp=character(), 
			catchCat=character(), 
			landCat=character(), 
			commCatScl=character(), 
			commCat=integer(), 
			subSampCat=character(), 
			sex=character(), 
			unitId=character(), 
			indSex=character(), 
			lenCls=integer(), 
			lenNum=integer(), 
			measType=character(), 
			measCls=character(), 
			measNum=character(), 
			convFacLen=character(), 
			fishTotal=character(), 
			fishSampled=character(), 
			fishSampProb=character()
		), 
		ca=data.frame(
			recType=character(), 
			seCode=character(), 
			year=integer(), 
			proj=character(), 
			trpCode=character(), 
			staNum=integer(), 
			foId=character(), 
			quarter=integer(), 
			month=integer(), 
			spp=character(), 
			sex=character(), 
			unitId=character(), 
			indSex=character(), 
			catchCat=character(), 
			landCat=character(), 
			commCatScl=character(), 
			commCat=integer(), 
			subSampCat=character(), 
			stock=character(), 
			area=character(), 
			rect=character(), 
			subRect=character(), 
			lenCls=integer(), 
			age=integer(), 
			fishId=integer(), 
			lenCode=integer(), 
			measType=character(), 
			measCls=character(), 
			fishAtLengthTotal=character(), 
			fishAtlengthSampled=character(), 
			individualFishSampProb=character(), 
			ageMeth=character(), 
			plusGrp=character(), 
			otoWt=double(), 
			otoSide=character(), 
			indWt=double(), 
			matMeth=character(), 
			matScale=character(), 
			matStage=character()
		)
	)
)

piPk <- function(table){
	if (table=="se") return(c("seCode"))
	if (table=="tr") return(c("seCode", "year", "proj", "trpCode"))
	if (table=="hh") return(c("seCode", "year", "proj", "trpCode", "staNum", "foId"))
	if (table=="sl") return(c("seCode", "year", "proj", "trpCode", "staNum", "foId", "spp", "catchCat", "landCat", "commCatScl", "commCat", "subSampCat", "sex", "unitId"))
	if (table=="hl") return(c("seCode", "year", "proj", "trpCode", "staNum", "foId", "spp", "catchCat", "landCat", "commCatScl", "commCat", "subSampCat", "sex", "unitId", "indSex", "lenCls", "measType", "measCls"))
	if (table=="ca") return(c("seCode", "year", "proj", "trpCode"))
}
